<%= @event.creator.name %>
<%= @event.description %>
<%= button_to "Attend", attend_event_path, method: :get %>
<%= @event.attendees.each do |attendee| %>
    <%= attendee.name %><br><br/>
    <% end %>